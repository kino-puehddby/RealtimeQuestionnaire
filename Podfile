platform :ios, '12.0'
use_frameworks!

# ignore all warnings from all pods
inhibit_all_warnings!

def install_pods
    # Resource
    pod 'SwiftGen', '~> 6.0.2'
    pod 'SwiftLint', '~> 0.27.0'

    # Firebase
    pod 'GoogleSignIn'
end

target 'RealtimeQuestionnaire' do
    # Pods for RealtimeQuestionnaire
    install_pods 
end
